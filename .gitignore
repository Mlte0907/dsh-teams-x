node_modules/
# lib/ 有意入库:GitHub 链接安装直接带成品(与 dsh-remote-x / dsh-pangu 一致);
# prepare 路线不可行——构建依赖未跟踪的本地 deepseek-harness 克隆。
.mimosa/
*.tsbuildinfo
deepseek-harness

# 本地交接文档不发布(内容已并入 ACCEPTANCE.md §六)
HANDOFF.md

# TeamsX 运行时状态（团队快照/信箱/归档），不入库
.teams-x/
