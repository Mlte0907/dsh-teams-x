/**
 * Locale key type for the `teamsX` namespace.
 *
 * The key set is derived from the zh dictionary in locales.ts (the source of
 * truth); this module is the stable import path for consumers, so the
 * dependency direction stays one-way: locale-keys → locales.
 * @module dsh-teams-x/client/locale-keys
 */
export {};
